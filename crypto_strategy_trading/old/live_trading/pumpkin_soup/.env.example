# ==========================================
# 交易机器人环境变量配置模板 (.env)
# 使用方法：复制此文件为 .env 并填入真实信息
# ==========================================

# ------------------------------------------
# 1. 交易所配置 (Exchange Configuration)
# ------------------------------------------
# 您的交易所 API Key。
# 注意：请确保 API Key 拥有 "交易 (Trade)" 权限，如果需要读取余额还需要 "读取 (Read)" 权限。
# 对应 config_live.json 中的 "exchange": {"name": "okx"}

OKX_API_KEY=your_okx_api_key_here
OKX_API_SECRET=your_okx_api_secret_here
OKX_PASSPHRASE=your_okx_passphrase_here

# 如果使用币安 (Binance)，请取消注释以下行并填写：
# BINANCE_API_KEY=your_binance_api_key_here
# BINANCE_API_SECRET=your_binance_api_secret_here

# ------------------------------------------
# 2. 飞书报警配置 (Feishu Alerting)
# ------------------------------------------
# 飞书机器人的 Webhook 地址。
# 获取方式：飞书群组 -> 设置 -> 群机器人 -> 添加机器人 -> 自定义机器人 -> 复制 Webhook 地址
# 机器人会发送：启动/停止通知、交易开平仓通知、错误报警。

FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ------------------------------------------
# 3. 数据库配置 (Database - Optional)
# ------------------------------------------
# 默认使用 SQLite (无需配置)，如果需要使用 MySQL，请填写以下信息：
# 并在启动时添加参数 --db mysql

# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=your_password
# MYSQL_DB=trading_bot
